spring:
  data:
    mongodb:
      authentication-database: admin
      username: test
      password: test
      database: twitter_test
      auto-index-creation: true
    
    redis:
      host: localhost
      port: 6379
      password:
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  
  cache:
    type: redis
    cache-names: posts,users,comments,likes,userPosts,followingPosts
    redis:
      time-to-live: 3600000

  security:
    user:
      name: test
      password: test

jwt:
  expiration: 600000
  secret: test-secret-key-for-testing-purposes-only-very-long-key-required
